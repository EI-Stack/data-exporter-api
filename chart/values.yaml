# Default values for chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: nexus-docker-registry.eistack100.iii-ei-stack.com/data_exporter
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "0.1.3"

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 5000

ingress:
  enabled: true
  className: "data-exporter"
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: data-exporter-v1.grant-174.iii-ei-stack.com
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources: {}
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 100
  targetCPUUtilizationPercentage: 80
  # targetMemoryUtilizationPercentage: 80

nodeSelector: {}

tolerations: []

affinity: {}


dataExporter:
  configmap:
    AFS_URL: http://api-afs-stage2-eks006.sa.wise-paas.com
    EKS_URL: https://ifp-organizer-dmd-eks009.sa.wise-paas.com/graphql
    FLASK_APP: data_exporter.run.py
    INSTANCE_ID: 2174f980-0fc1-5b88-913b-2db9c1deccc5
    MONGODB_HOST: 127.0.0.1
    MONGODB_PORT: "27017"
    MONGODB_TABLE_NAME: data_set
    MQTT_BROKER_PORT: "31883"
    MQTT_BROKER_URL: e150-01.eistack100.iii-ei-stack.com
    MQTT_KEEPALIVE: "5"
    MQTT_PASSWORD: g0kBqOviZ8
    MQTT_REFRESH_TIME: "1.0"
    MQTT_TLS_ENABLED: "False"
    MQTT_USERNAME: iii-eistack
    S3_ACCESS_KEY: Bknu1IKIfK5it1XnseDh4GsuzwhAG1JF
    S3_ENDPOINT: ai-storage.amp.iii-ei-stack.com
    S3_SECRET_KEY: mSM8eEYrT57votdHj7BPmZdxvN5hSb3I
    SSO_TOKEN: https://ifps-predict-train-ifpsdev-eks005.sa.wise-paas.com/api/v1/token
    X_IFP_APP_SECRET: OWFhYThkZWEtOGFjZS0xMWViLTk4MzItMTZmODFiNTM3OTI4


